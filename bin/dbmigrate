#!/bin/bash

working=$PWD

for (( i=1; i<=$#; i++)); do

    case "${!i}" in
        -w | --working)
            j=$((i+1))
            working="${!j}"
            ;;
    esac
done
if ! [ -d "$working" ]; then
    working=$PWD
fi

docker run --rm -v $working:/changelogs dbmigrate $@
